# 040: Теор. слой для задач апстрима (track: core, постфактум)

## Статус
Реализован до написания спека (ветка spec/040-upstream-theory);
спек фиксирует границы и приёмку задним числом.

## Что сделано / досделать
Каждому template апстрима добавлены: теор. блок (формулы, план
реализации, быстрые самопроверки, частые ошибки), секция «Материалы
для глубокого изучения» (2–3 конкретные ссылки), секция «Вопросы для
самопроверки» (без ответов).
Досделать: 11_sliding_window, 24_rope, 34_speculative_decoding,
00_welcome (пара строк о формате теор. блоков).

## Границы
Только templates/*.ipynb. Не трогать: torch_judge/, tests/, solutions/,
specs/. Язык допускается смешанный (русские вставки поверх английского
апстрима) — осознанное решение.

## Приёмка
- ls templates/*.ipynb — все 41 файл содержат секцию «Материалы»
  (проверка: rg -l "Материалы для глубокого изучения" templates/ | wc -l → 40,
  без 00_welcome)
- Выборочные ноутбуки исполняются: jupyter nbconvert --execute на
  02, 14, 24, 39 — без ошибок
- pytest -q — зелёный (ничего вне templates не изменено)