# 030: Треки в реестре задач + оглавление (track: infra)

## Зависит от
000-conventions, 010-hints-engine

## Что сделать
- Поле `track: str` в TASK (опциональное; задачи апстрима без поля
  считаются track="core").
- В `torch_judge/tasks/_registry.py` добавить `list_tasks(track: str |
  None = None)` — фильтрация; без аргумента поведение прежнее (все задачи,
  сортировка по difficulty). Сигнатуру существующей функции не ломать
  (аргумент со значением по умолчанию).
- `templates/00_tracks_index.ipynb` — оглавление: таблица треков
  (core / distributed / inference / nanogpt / positional), для каждого —
  цель трека в 1–2 строках, порядок ноутбуков, статус (какие спеки уже
  реализованы — поддерживать при последующих PR).
- README репозитория: секция «Tracks» с той же таблицей + строка «чем
  этот форк отличается от апстрима» (hints, llm-judge, треки).

## Приёмка
- `pytest tests/test_registry_tracks.py -q` — зелёный. Тесты:
  (1) `list_tasks()` возвращает все задачи, включая апстримные;
  (2) `list_tasks(track="distributed")` — только помеченные;
  (3) задача без поля track попадает в track="core".
- `pytest -q` весь репозиторий — зелёный.
- `jupyter nbconvert --to notebook --execute templates/00_tracks_index.ipynb`
  — без ошибок.

## Не делать
Не перемещать файлы задач апстрима по подкаталогам (авто-дискавери
завязан на плоскую структуру) — трек задаётся только полем.
